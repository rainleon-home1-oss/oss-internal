#!/usr/bin/env bash
# just descript the token access files in gitlab private repo
# for obtaining this private_token,you should copy from http://gitlab.XXXXX-> Profile Settings->Account->Private Token
# or `curl --request POST "http://gitlab.XXXX/api/v3/session?login={邮箱}&password={密码}"`
export GIT_SERVICE_TOKEN=
